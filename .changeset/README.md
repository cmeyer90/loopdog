# Changesets

User-visible changes need a changeset: run `npx changeset`, pick the bump level,
and write a short human note. All `@loopdog/*` packages move on one fixed version
line; only `@loopdog/cli` is published (the libraries are bundled into it).
See CONTRIBUTING.md "Releases & changesets".
